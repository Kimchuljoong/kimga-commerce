server:
  port: 8083

spring:

  application:
    name: order

  datasource:
    url: jdbc:mysql://localhost:3307/kimga_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: kimga
    password: kimga1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
